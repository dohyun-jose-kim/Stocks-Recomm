# 2026-07-08 바이브 코딩 로그

세션 한 번에 일어난 일을 시간 순서대로 기록. 결정과 그 이유를 위주로.

---

## 0. 시작 상태

- 목적: 한 달 방치 후 정비 — GitHub sync, 문서 현행화(stale 표기), VM cron 상태의 상세 문서화. **코드는 건드리지 않음.**
- 로컬: origin보다 24커밋 뒤(VM 스냅샷) + 7/2 작업 4커밋이 미push 상태로 diverge + 미커밋 작업(TODO.md, report.py v0.1.1, CLAUDE_dh_v1.md) 존재.
- 삽질 하나: 이 정비를 처음에 06_Agent_Collaboration 레포에서 진행하는 오인이 있었음 → 그쪽은 revert(a4647c3)하고 여기로 재수행.

## 1. VM 상태 실사 (SSH)

**확인 순서**: `ls` → `crontab -l` → `fetch.log` → `git remote/log`.

- 크론 원문: `0 0,6,12,18 * * * /bin/bash ~/01_/Stocks-Recomm/scripts/run_fetch.sh >> ~/fetch.log 2>&1` — 셋업(6/5) 그대로.
- **한 달 무인 완주 확인**: 07-08T00:00Z 런까지 수집·push 연속 성공. 사람 손 0.
- 크레딧: ₩448,276 잔여 (월 ~₩520 소진) — Always Free 사양이 제대로 잡힘. 9/4 만료 시 잔액 소멸(이월 없음)임을 재확인.
- **신규 발견**: push 로그에 GH001 대용량 경고. DB 57.7MB, ~1.2MB/일 성장 → **8월 중순 100MB 도달 시 push 거부 = 백업 조용히 끊김.** 9/4보다 먼저 오는 데드라인.

## 2. GitHub sync (merge 결정)

**결정**: rebase 대신 merge — 로컬 4커밋과 origin 24커밋(스냅샷)이 만진 파일이 안 겹침을 확인(`comm` 검증) + 미커밋 dirty 파일이 있어 rebase는 부적합. merge 커밋 c132802.

- VM의 `git pull --ff-only`는 merge 커밋이 origin/main을 조상으로 포함하므로 안 깨짐 (push 후 다음 크론 런이 실증 예정).
- push 완료: 7/2 작업(v0.1.0 탐색 MVP 포함)이 처음으로 origin 반영. tag `v0.1.0`도 push.

## 3. 문서 현행화 (무차별 갱신 대신 stale 표기)

**원칙**: 원문 보존, 차이만 표기. 갱신 = README/PROJECT_OVERVIEW(“계획 단계, 코드 없음” → 현행), stale 배너 = docs/github_actions.md(Actions 방식 폐기 경위 명시). vm_cron_migration/README.md에 “상태 점검 (2026-07-08)” 절 신설 — 다음 점검도 같은 절에 이어 쓰는 구조.

- `docs/KNOWN_LIMITATIONS.md` 생성 (CLAUDE.md §1의 “생기면 생성” 첫 발동): DB 100MB 리스크 등재.
- PLAN.md 현재 좌표에 운영 데드라인 2건 추가 (8월 중순 DB / 9월 초 체험판).

## 4. 이슈 등록

- [#8](https://github.com/dohyun-jose-kim/Stocks-Recomm/issues/8) 체험판 만료(9/4) 대응 — pay-as-you-go 업그레이드 결정, 8월 초까지.
- [#9](https://github.com/dohyun-jose-kim/Stocks-Recomm/issues/9) 대시보드 — 가능한 일, 단 PLAN.md 조건(리포트를 매일 보게 되면) 충족 시 재검토하는 백로그로.
- DB 100MB 건은 KNOWN_LIMITATIONS에 기록 (이슈화 여부는 사용자 결정 대기).

## 5. 끝 상태 / 다음 진입점

- 미커밋으로 남긴 것 (의도적 — 코드 불변 원칙): `src/explore/report.py`+`TODO.md`(v0.1.1 이벤트 오버레이, HASH_PENDING), `CLAUDE_dh_v1.md`(untracked). **다음 세션에서 v0.1.1을 검증·커밋하는 게 첫 일.**
- 운영 데드라인: ① 8월 중순 전 DB 저장 방식 결정 ② 8월 초 #8 실행 ③ PAT 만료일 확인·기록 (아직 미기록).
