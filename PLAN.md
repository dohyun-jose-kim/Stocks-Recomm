# PLAN.md — 마스터 설계

> 갱신 시점: 설계 결정이 바뀔 때. 세부 배경은 `plans/` (초기 계획 문서, 참고용 보존).

## 현재 좌표 (2026-07-02)

- 수집 파이프라인은 GCP VM cron으로 6시간 간격 정상 가동 중. **손대지 않는다.**
- 쌓인 데이터: 스냅샷 ~2만 건, 마켓 140개 (Kalshi 109 / Polymarket 31), 2026-05-21 ~ 현재.
- 병목은 수집이 아니라 **소비** — `src/explore/`가 비어 있어 데이터를 보는 눈이 없다.
- ⚠️ (2026-07-08 추가) "손대지 않는다"에 예외가 생길 예정: DB 파일이 GitHub 100MB 한도로 성장 중, 8월 중순 push 거부 예상 → 저장 방식 결정 필요 (#10 — 수집 확장 전제로 재설계). 운영 데드라인 2건 = ① DB 100MB #10 (8월 중순) ② 체험판 만료 #8 (9/4, 대응은 8월 초). 상세: `docs/KNOWN_LIMITATIONS.md`.

## 방향 (결정: 2026-07-02)

순서 고정: **① 보는 도구(탐색 MVP) → ② 수집 커버리지 확장 → ③ (필요해지면) 리포트 자동화.**
대시보드 프레임워크는 만들지 않는다. CLI 스크립트 한 개로 시작.

## 버전 로드맵

### v0.1.0 — 탐색 MVP (급변 감지 + 추이 차트)

산출물: `src/explore/report.py` (CLI 스크립트 1개)

1. **급변 감지**: 각 마켓의 최신 yes_price를 24h(및 7d) 전 값과 비교,
   |Δ| ≥ 임계값(기본 0.10, 옵션으로 조정) 마켓을 변동폭 순으로 출력.
2. **추이 차트**: 지정 마켓(또는 급변 상위 N개)의 확률 시계열을 matplotlib PNG로
   `data/reports/`에 저장 (gitignore 대상).

검증 기준:
- 실제 DB에 대해 실행 시 급변 후보가 리스트로 출력된다 (6주 데이터 기준 3개 이상 기대).
- 임의 마켓 1개 이상의 추이 PNG가 생성되고 눈으로 확인 가능하다.

의존성 추가: `matplotlib` (requirements.txt).

### v0.2.0 — 커버리지 확장 (후속, 별도 계획)

`src/config/markets.yaml` 화이트리스트 확대 (KXCPI, KXJOBS, 지정학 등).
탐색 도구로 실제 사용해본 뒤 착수.

## 하지 않는 것 (스코프 밖)

- 자동매매, 퀀트 팩터, 예측 정확도 평가 (PROJECT_OVERVIEW.md 참조)
- Streamlit 등 대시보드 — 매일 보게 됐을 때만 재검토
- 기존 수집기/스키마 변경

## 구조 메모

기존 `src/` 레이아웃(collector/storage/explore/config)은 이미 확립된 구조이므로 유지한다.
CLAUDE.md §3의 `NN_` 디렉터리 컨벤션은 이 리포에 소급 적용하지 않는다.
